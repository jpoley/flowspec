

# KEY FOLDERS

# RULE

./build-docs folder is for internal use of BUILDING flowspec (these docs shouldn't be published OR installed in users installation)

./user-docs folder is intended for consumsers / users of flowspec. and this should 100% be published and clear for every aspect of using flowspec

./backlog is the backlog for building flowspec (not for end users)

# adr

for all architectural decisions that have been made about flowspec

# building

this is were we should keep ALL documents around building flowspec (not intended for user / consumers of) this is legacy and can be refactored out to a flatter structure

# prd

this is where we keep the product requirements docs (for building) 


# specs

this is where we keep the specifications we create. 

